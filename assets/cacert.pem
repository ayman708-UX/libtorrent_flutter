# Mozilla CA Certificate Store
# This file is a placeholder. Replace with the real Mozilla CA bundle.
# Download from: https://curl.se/ca/cacert.pem
# 
# The CI workflow downloads the real bundle automatically.
# For local Android development, run:
#   curl -o assets/cacert.pem https://curl.se/ca/cacert.pem
#
# DO NOT commit the real cacert.pem — it is downloaded by CI.
# This placeholder will cause lt_create_session to throw on Android
# until the real bundle is placed here.
